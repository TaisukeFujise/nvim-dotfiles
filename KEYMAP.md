# Neovim キーマップ マニュアル

リーダーキー: `Space`

---

## 起動

```bash
nvim .        # カレントディレクトリをoil.nvimで開く
nvim <file>   # ファイルを直接開く
```

---

## モード切替

| キー | 動作 |
|------|------|
| `jj` | Insert → Normal |
| `i` | Normal → Insert |
| `v` | Normal → Visual |
| `Esc` | どこからでも Normal へ |

---

## ウィンドウ・分割

| キー | 動作 |
|------|------|
| `ss` | 横分割して移動 |
| `sv` | 縦分割して移動 |
| `<CR><CR>` | 次のウィンドウへ移動 |

---

## ファイル操作（oil.nvim）

`nvim .` で開いたときのバッファ内ファイラ。

| キー | 動作 |
|------|------|
| `Enter` | ファイルを開く / ディレクトリに入る |
| `-` | 親ディレクトリへ移動 |
| `_` | カレントファイルのディレクトリを開く |
| `g.` | 隠しファイルの表示切替 |
| `gs` | ソート順の変更 |
| `g?` | oil.nvim のヘルプ |
| `<C-s>` | 縦分割で開く |
| `<C-h>` | 横分割で開く |
| `<C-p>` | プレビュー |
| `q` | 閉じる |

> ファイル名の変更・削除・移動はバッファを直接編集して `:w` で確定。

---

## ピッカー（snacks.nvim）

すべてカレントバッファのディレクトリ基点で動作。

| キー | 動作 |
|------|------|
| `<leader>ff` | ファイル検索 |
| `<leader>fg` | ライブ grep |
| `<leader>fr` | 最近開いたファイル |
| `<leader>fp` | プロジェクト一覧（~/Cursus, ~/Road, ~/Documents） |
| `<leader>fe` | ファイルエクスプローラ |

ピッカー内の操作:

| キー | 動作 |
|------|------|
| `Enter` | 選択して開く |
| `<C-s>` | 横分割で開く |
| `<C-v>` | 縦分割で開く |
| `Esc` | 閉じる |

---

## LSP

| キー | 動作 |
|------|------|
| `gd` | 定義へジャンプ |
| `gI` | 実装へジャンプ |
| `K` | ホバー（型情報・ドキュメント表示） |

対応言語: Lua, C/C++ (clangd), Go (gopls)

---

## Git

| キー | 動作 |
|------|------|
| `<leader>lg` | lazygit を開く（要インストール: `brew install lazygit`） |
| `<leader>gd` | Git diff のハンク一覧 |

---

## ターミナル

| キー | 動作 |
|------|------|
| `<C-\>` | 浮動ターミナルのトグル（カレントバッファのディレクトリで起動） |

ターミナル内で `exit` または `<C-\>` で閉じる。

---

## コメント（Comment.nvim）

| キー | 動作 |
|------|------|
| `gcc` | 現在行をコメントトグル |
| `gc` + モーション | 範囲をコメントトグル（例: `gc3j` で3行） |
| `gc` | Visual モードで選択範囲をコメントトグル |

---

## 42 Header

| キー | 動作 |
|------|------|
| `<F1>` | ヘッダーの挿入・更新 |

保存時に自動更新される。

---

## その他

| キー | 動作 |
|------|------|
| `<leader>?` | バッファのキーマップ一覧を表示（which-key） |
| `<leader>R` | リーダーモードのトグル |
| `j` / `k` | 長押しで加速スクロール（accelerated-jk） |
